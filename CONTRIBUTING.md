# Contributing

Keep the core provider-neutral and deterministic. New assertion types need passing, failing, and malformed-input tests. Avoid shell interpolation and never log environment variables or credentials.
