# Security policy

EvalCase executes the command supplied by its user. Suites are data, not commands, and are never evaluated as shell input. Only run adapters you trust. Report command-boundary vulnerabilities through GitHub Security Advisories.
